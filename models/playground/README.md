# Playground Models

Use this folder for AI-generated or student-generated models.

Examples:

- `models/playground/rpt_daily_revenue.sql`
- `models/playground/rpt_country_performance.sql`

Tip: Ask the AI to create models here first, then move stable models to `marts/`.
