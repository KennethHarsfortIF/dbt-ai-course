# Answer Key 3: Improve Model

High-priority improvement examples:

1. Add relationship tests on key foreign keys in marts where missing
2. Add explicit handling for null monetary fields in aggregates
3. Add model-level descriptions for each marts model and key columns

Medium-priority examples:

1. Standardize boolean logic naming (`is_*`)
2. Add one singular data quality test per critical model
